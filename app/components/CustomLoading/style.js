import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  absolute: {
    position: 'absolute',
    top: 20,
    bottom: 0,
    left: '20%',
    right: 0,
    zIndex: 1,
  },
});

export default styles;
