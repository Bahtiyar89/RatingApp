import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  text: {
    marginTop: 20,
    fontFamily: 'Nunito',
    fontWeight: '700',
    fontSize: 32,
    lineHeight: 34,
  }
});

export default styles;
